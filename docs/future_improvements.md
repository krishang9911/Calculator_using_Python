# Future Improvements

Possible enhancements for the calculator project:

- Add more operations (modulus, exponent, square root).
- Add support for continuous calculations (use previous result automatically).
- Create a GUI version using Tkinter.
- Add unit tests using `unittest` or `pytest`.
- Improve input validation and error messages.
- Turn the script into a small package with multiple modules.
